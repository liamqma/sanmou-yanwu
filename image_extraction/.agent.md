Use the uv-managed Python 3.12 environment; dependencies are pinned in
`image_extraction/pyproject.toml` and `uv.lock`. Run `make extract` to extract
and `make test` to test. OCR settings come from `extraction_config.json` under
`ocr_settings`.

# Optional cloned-voice reference

Manual human narration is the default workflow and does not use this folder.

Only when cloned-voice TTS is explicitly requested, place these local files
here:

- `voice.m4a` — the reference recording
- `ref.txt` — an exact transcript of that recording

Both files are ignored by Git. Never commit personal voice recordings or their
transcripts.

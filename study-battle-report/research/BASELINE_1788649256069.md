# Baseline: battle 1788649256069

This is the tracked review summary for the full real-OCR v2 run of the
standalone battle-damage research pipeline. Detailed JSON/JSONL artifacts are
regenerable and ignored under `research/results/1788649256069/`.

## Audit boundary

- Source: 25 unchanged screenshots, a 654-line stitched log, its OCR v2 cache,
  and the complete v2 observation-lineage sidecar.
- Input log SHA-256:
  `8a06f1152068645a32c30d55a66a36881330d8e6004ae759aa5b92235005cb56`.
- Input cache SHA-256:
  `f498588bb097944314aa6a6fb9f4768a74c2c357ede0fb9165c95936990c707e`.
- Input provenance-sidecar SHA-256:
  `75e5227fe172c884d467f65514131e0be4e99634c6ba5106a35588f050188572`.
- The sidecar declares the same log and cache hashes, is pinned by the tracked
  manifest, and reports `observation_provenance_complete=true`.
- The regenerated source-set hash is
  `ff8976bc162a39dd6a457fe729d3eb02c969d40da2e5dc35fdefc67ff7e8ddae`,
  the pipeline-code hash is
  `0910b6c76ac2feccd793bd423b068ccc111742e7e8bba681c584bc948695bfe6`,
  and the validated artifact-set hash is
  `c3f5fb9e0606fce3f00eefd39225801963ea12ece6ad8969eb6fb39922c73180`.
- The cache contains 25 frames and 1,162 observations. Twenty-four frames have
  direct OCR; `battle_detail_1788649490796.png` conservatively reuses 43
  observations from the single detected near-duplicate frame
  `battle_detail_1788649481018.png`.
- All 1,162 observations retain string raw/processed text and a four-point
  bounding box. There are 780 token-colour evidence records: 324 resolve to
  我方, 274 to 敌方, and 182 remain unknown (178 ambiguous-colour and 4
  insufficient-pixel decisions). Unknown decisions remain null.
- The deterministic output contains 654 line observations and exactly 654
  events, including 212 `unknown` events whose original text and line number
  remain present.
- The report marks 2 proven lethal damage events (`L530`, `L633`) as
  right-censored. The malformed `L652` post-hit value is absent from the parsed
  event, is not otherwise proven lethal, and therefore is not classified as
  equality or right censoring (its mirror identity also keeps replay skipped).
- 乐进 and 糜夫人 are mirror names. Their observed tags remain visible, but
  their resolved side is null and 117 potentially state-changing rows are
  skipped rather than merged across teams.
- The sidecar records 3,254 transformations: 2,651 exact mappings and 603
  heuristic mappings. Final lineage is `deterministic_v2` for 242 lines and
  `deterministic_heuristic_v2` for 412 lines. Observation linkage is exact for
  all 654 final lines, but heuristic text/stitch decisions remain explicit and
  are not promoted to exact damage evidence. Side provenance is retained for
  each entity occurrence: 410 direct token-colour, 86 inferred backfill, and 66
  missing-side occurrences.

## Reproducible quality summary

| Item | Count |
|---|---:|
| Exact v2 observation-lineage alignment | 654 |
| Final deterministic-v2 lineage | 242 |
| Final deterministic-heuristic-v2 lineage | 412 |
| Token-colour evidence records | 780 |
| Unresolved token decisions | 182 |
| Parsed events | 352 |
| Partial events | 90 |
| Unknown events | 212 |
| Mirror-ambiguous events | 213 |
| Inferred-side events | 59 |
| State snapshots | 306 |
| Non-censored damage rows with complete actor, target, skill, and clean provenance | 0 |

Replay applies 108 snapshots, skips 117 mirror-ambiguous identity mutations,
skips 47 inferred-side mutations, and skips 34 snapshots with missing identity.
Only `我方:夏侯渊` and `敌方:祝融` are tracked as unambiguous subjects.

The 69 parsed or partially parsed damage rows remain conservatively partitioned:

| Damage eligibility | Count |
|---|---:|
| Right-censored lethal likelihood only | 2 |
| Excluded mirror side | 38 |
| Excluded partial parse | 24 |
| Excluded unresolved causal side | 4 |
| Excluded heuristic provenance | 1 |
| Eligible exact damage | 0 |

Prominent retained event anomaly flags are mirror-side ambiguity (213),
fragmented entries (30), truncated numbers (12), reconnect/overlay
contamination (2), and a same-side attack suspect (1). Token geometry remains
explicitly approximate on 488 final lines, 143 lines retain one or more
unresolved token sides, 85 lines contain at least one inferred entity side, and
412 lines retain fuzzy-stitch/repair uncertainty.

No exact duplicate line pair remains within a 45-line window and there are no
adjacent exact duplicates. Eleven high-similarity pairs remain under the fuzzy
stitch normalizer; the opening pairs are distinct heroes/sides or distinct stat
updates, while `L603`/`L617` may be repeated 南疆烈刃 actions and the latter
retains the OCR form `执行来自自`. They are retained rather than silently
dropped. The sole result is the terminal `L654` victory line, the first line is
the action-order result, and the line count remains 654, so no gross head/tail
truncation was observed. Local OCR damage remains visible, including 12
truncated-number flags, 30 fragmented-event flags, the noise line `L526` (`入入`),
and malformed fragments around `L590`–`L635`; these rows stay excluded or
unknown and require manual review before any stronger inference.

## Displayed percentage accumulator

The fixed UI candidates are evaluated separately from final damage. Among 24
state-replay transitions with a known previous display, delta, and new display,
the hidden-precision candidate must use one shared clipping-bound pair across
the full evaluated transition set:

| Candidate | Compatible | Violations |
|---|---:|---:|
| exact displayed-value addition | 17 | 7 |
| hidden-precision clipped addition with two-decimal display | 19 | 5 |
| simple signed independent multiplication | 0 | 24 |

The best shared hidden-precision bounds are `L=-162.48` and `U=95.01`, with
five counterexamples that cannot each choose a different cap. Those violations
are not treated as decisive final-formula counterexamples: several may have a
missing or unassignable preceding component in the OCR log. Directly auditable
examples still show why display rounding must be modelled. These UI checks make
no claim about the composition used by final damage.

## Final-damage result

`final_damage.status = insufficient_independent_groups`

There is one independent capture session. The pipeline selects no final damage
formula, fits no parameter, computes no residual/CV score or confidence
interval, and sets `restored_formula_claim` to false. All 18 registered final
damage candidates remain `not_evaluated_insufficient_independent_groups`, and
the exact-damage fitting population is zero after provenance and parsing gates.
The observed 90% per-hit reduction at `L406` is descriptive evidence only; it
cannot distinguish a hard cap, soft cap, display rounding, or
total-versus-component resistance semantics.

## Data still required

1. Three independent sessions in each of eight controlled cells: actual
   displayed incoming reduction around 0–10%, 40–55%, 70–80%, and at least 90%,
   each both without and with resistance (24 qualifying reports).
2. Equal-total component tests: one large reduction versus two smaller
   reductions, plus reversed application order; three sessions per setup.
3. For every report: capture time, game build/season, hero and tactic levels,
   equipment and strategy, formation/row, pre-battle attributes, full loadouts,
   and supply. The generated gap now names only fields absent from each source
   manifest and disappears when every required field is complete.
4. Prefer non-mirror lineups, non-critical first-round hits, stable attacker and
   target, and non-lethal observations.

Until those gaps are filled, the exact base-damage function, modifier channels,
resistance composition, and near-cap behaviour remain unidentifiable.
